apiVersion: v1
kind: Service

metadata:
  name: dummypro-service
  namespace: dummy

spec:
  selector:
    app: dummypro

  type: NodePort

  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080